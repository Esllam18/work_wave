import 'package:flutter/material.dart';

class SevedView extends StatelessWidget {
  const SevedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Saved Jobs'));
  }
}
